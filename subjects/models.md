# Model Sets — Class Project (NestJS Backend API)

เอกสารนี้ใช้สำหรับกำหนด **ชุด Core Data Model** สำหรับแต่ละกลุ่ม

> 📌 หมายเหตุ
>
> * แต่ละกลุ่มเลือกใช้ได้ **1 ชุดเท่านั้น** (ห้ามซ้ำกัน)
> * แสดงเฉพาะ *Core Model*
> * Model ย่อย, attribute, enum และความสัมพันธ์เชิงลึก ให้นักศึกษาออกแบบเองทั้งหมด

---

## 🧩 Model Set 0: User & Organization Management

**Core Models**

* User
* Organization

**Description**
ระบบจัดการผู้ใช้งานภายใต้องค์กร เหมาะสำหรับฝึกการออกแบบความสัมพันธ์เชิงโครงสร้าง และการจัดการผู้ใช้ในบริบทองค์กร

---

## 🧩 Model Set 1: Blog / Content Platform

**Core Models**

* Post
* Comment

**Description**
ระบบจัดการเนื้อหาและความคิดเห็น เหมาะสำหรับฝึก CRUD และความสัมพันธ์ระหว่างข้อมูล

---

## 🧩 Model Set 2: Course Registration System

**Core Models**

* Student
* Course

**Description**
ระบบลงทะเบียนเรียน เหมาะสำหรับฝึกการออกแบบ relationship และ business rule พื้นฐาน

---

## 🧩 Model Set 3: Event Management System

**Core Models**

* Event
* Participant

**Description**
ระบบจัดกิจกรรมและผู้เข้าร่วม เน้นการจัดการสถานะและช่วงเวลา

---

## 🧩 Model Set 4: Task & Project Tracking

**Core Models**

* Project
* Task

**Description**
ระบบติดตามงานและโปรเจค เหมาะสำหรับฝึก workflow และ state-based logic

---

## 🧩 Model Set 5: E-commerce Basic

**Core Models**

* Product
* Order

**Description**
ระบบร้านค้าออนไลน์ขั้นพื้นฐาน เหมาะสำหรับฝึก business logic และการจัดการข้อมูลเชิงธุรกรรม

---

## 🧩 Model Set 6: Appointment Booking System

**Core Models**

* Service
* Appointment

**Description**
ระบบจองบริการหรือคิว เหมาะสำหรับฝึก validation เชิงเงื่อนไขและการจัดการเวลา

---

## 🧩 Model Set 7: Inventory Management

**Core Models**

* InventoryItem
* Warehouse

**Description**
ระบบคลังสินค้า เหมาะสำหรับฝึกการจัดการข้อมูลเชิงปริมาณและสถานะ

---

## 🧩 Model Set 8: Support / Ticket System

**Core Models**

* Ticket
* Customer

**Description**
ระบบรับแจ้งปัญหาและติดตามสถานะ เหมาะสำหรับฝึก enum (status), workflow และ error handling

---

## 🧩 Model Set 9: Library Management System

**Core Models**

* Book
* Member

**Description**
ระบบห้องสมุดพื้นฐาน เหมาะสำหรับฝึกการออกแบบความสัมพันธ์และ business rule ที่เข้าใจง่าย

---

## 🎯 วิธีการเลือก Model Set สำหรับแต่ละกลุ่ม

เพื่อป้องกันความซ้ำซ้อนของโจทย์ และให้การเลือกเป็นธรรม ให้แต่ละกลุ่มเลือก Model Set ตามขั้นตอนดังนี้:

1. นำ **Student ID ของสมาชิกทุกคนในกลุ่มมารวมกัน** (ผลรวมเป็นตัวเลข)
2. นำผลรวมที่ได้ **mod 10**
3. ค่าที่ได้ (0–9) ให้นำมาเทียบกับ Model Set ดังนี้:

| ค่า mod 10 | Model Set   |
| ---------- | ----------- |
| 0          | Model Set 0 |
| 1          | Model Set 1 |
| 2          | Model Set 2 |
| 3          | Model Set 3 |
| 4          | Model Set 4 |
| 5          | Model Set 5 |
| 6          | Model Set 6 |
| 7          | Model Set 7 |
| 8          | Model Set 8 |
| 9          | Model Set 9 |

> 📌 เมื่อได้ Model Set แล้ว **ห้ามเปลี่ยน** เว้นแต่ได้รับอนุญาตจากอาจารย์

---

## 📦 การบันทึก Model Set ที่เลือกไว้ในโปรเจค

หลังจากคำนวณและเลือก Model Set แล้ว ให้บันทึกข้อมูลดังกล่าวไว้ในไฟล์ `package.json` ของโปรเจค

### รูปแบบที่กำหนด

```json
{
  "projectModelSet": {
    "set": 6,
    "name": "Event Management System",
    "totalStudentId": 64123456
  }
}
```

* ค่า `set` คือหมายเลข Model Set ที่ได้รับ
* ค่า `name` คือชื่อ Model Set ตามที่ระบุในเอกสารนี้

---

## 📌 Rules & Constraints

* ทุกกลุ่มสามารถใช้ได้เพียง **1 Model Set เท่านั้น**
* ห้ามซ้ำกันระหว่างกลุ่ม
* ทุก Model ต้องรองรับ CRUD Operation
* ต้องมี Enum อย่างน้อย 1 จุดในระบบ
* ❌ **ห้ามใช้ Type `object` โดยตรงใน Data Model หรือ DTO**
* หากข้อมูลมีโครงสร้างซับซ้อน ต้องประกาศเป็น **`interface` หรือ `type` แยกต่างหาก** แล้วนำมาอ้างอิงแทน
* ห้ามใช้ `any` type ในทุกกรณี
* ต้องกำหนด interface แบบ narrow type สำหรับ request และ response
* ต้องจัดทำ UML Diagram สำหรับ Core Model ที่เลือกใช้

---

## 📐 Guideline การออกแบบ Attribute (สำคัญ)

เพื่อให้การออกแบบ Data Model มีคุณภาพ และสะท้อนความเข้าใจเชิงระบบ
**แต่ละ Model ต้องมี Attribute อย่างน้อย 10 รายการ** และควรออกแบบโดยอิงตามแนวทางต่อไปนี้

### 1. Identity & Reference

* Attribute สำหรับระบุตัวตนของ Model (เช่น id, code)
* ควรมีความชัดเจนและไม่ซ้ำกัน

### 2. Core Domain Information

* Attribute ที่สะท้อนข้อมูลหลักของ domain
* ต้องมีความหมายเชิงธุรกิจ ไม่ควรเป็นข้อมูลทั่วไปที่ไม่จำเป็น

### 3. Status / State

* Attribute สำหรับบอกสถานะของ Model
* **ควรใช้ enum** เพื่อควบคุมค่าที่เป็นไปได้

### 4. Relation & Association

* Attribute ที่อ้างอิงถึง Model อื่น (เชิงตรรกะ)
* หากเป็นโครงสร้างข้อมูล ให้ใช้ interface แยก

### 5. Configuration / Flag

* Attribute สำหรับกำหนดเงื่อนไข หรือการตั้งค่า
* มักอยู่ในรูปแบบ boolean หรือ enum

### 6. Timestamp / Metadata

* Attribute เกี่ยวกับเวลา หรือข้อมูลกำกับระบบ
* เช่น เวลาสร้าง, เวลาแก้ไข, ผู้กระทำ

> 📌 หมายเหตุ
>
> * ไม่จำเป็นต้องมี Attribute ครบทุกหมวดในทุก Model แต่ **ภาพรวมต้องสมเหตุสมผล**
> * Attribute ทุกตัวต้องสามารถอธิบายเหตุผลในการมีอยู่ได้

---

### ตัวอย่างที่ไม่เหมาะสม ❌

```ts
extra1: string;
extra2: string;
extra3: string;
```

### ตัวอย่างแนวคิดที่เหมาะสม ✅

```ts
status: OrderStatus;
createdAt: string;
updatedBy: string;
```
